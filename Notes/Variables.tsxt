variables are the name of the container for a particular set of bits or type of data .

variable name is the usual way to reference the stored value

naming a variable ---- 


LEGB Rule ---- Local Enclosing Global built in pe kaam karta hai i.e. 